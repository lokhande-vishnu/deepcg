function parsave_eval(filename, dataset, log_lambda, layer, log_eta, ...
    param_local, prediction_on_test, training_values)
    save(filename, 'dataset', 'log_lambda', 'layer', 'log_eta', ...
        'param_local', 'prediction_on_test', 'training_values');
end
